import numpy as np
from datetime import timedelta
from nutshellml.model.data_model import TickData

"""
tick data is in the format of
"""

def time_sampling(tick_data : TickData, frequency : timedelta = timedelta(seconds = 1)):
    pass